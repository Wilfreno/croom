/**
 * Metadata key used by the `@ResponseMessage()` decorator.
 */
export const RESPONSE_MESSAGE_KEY = 'server_response:message';

/**
 * Metadata key used by the `@ResponseStatus()` decorator.
 */
export const RESPONSE_STATUS_KEY = 'server_response:status';
