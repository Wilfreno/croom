import { RoomInvite } from "@/lib/types/client-types";

export default function RoomInviteNotification({
  room_invite,
}: {
  room_invite: RoomInvite;
}) {
  return <div>RoomInviteNotification</div>;
}
